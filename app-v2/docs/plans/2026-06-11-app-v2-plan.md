# App V2 — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: flutter-craft:flutter-executing (batch). Design em
> `2026-06-11-app-v2-design.md`.

**Goal:** Portar o conceito da LP V2 (liturgia digital) para o app em `app-v2/`.

**Architecture:** Clean Architecture existente (Riverpod + go_router). Sem camada nova de
domain/data — V2 é presentation-only + 1 função pura.

**Dependencies:** nenhuma nova.

---

## Task 1 — Infra do fork
- Modify: `.gitignore` → ignorar `assets/audios/` (62 MB já versionados em `app/`)
- Modify: `setup.sh` → sincronizar áudios de `../app/assets/audios`
- Modify: `android/app/src/main/AndroidManifest.xml` → label "O meu Salmo V2"
- Verificação: `git check-ignore assets/audios/salmo_001.mp3`

## Task 2 — Widgets compartilhados (Presentation)
- Create: `lib/shared/widgets/starfield_background.dart` — CustomPainter, ~70 partículas,
  drift vertical + twinkle senoidal, cores por brilho do tema, desliga em
  `MediaQuery.disableAnimations`, RepaintBoundary.
- Create: `lib/shared/widgets/staggered_entrance.dart` — fade + translateY(26→0),
  `delay = index * 80ms`, dispara no initState (pós-frame).
- Create: `lib/shared/widgets/word_reveal_text.dart` — TextSpans por palavra, 1
  AnimationController, `Interval` escalonado, fallback instantâneo sem animação.
- Create: `lib/shared/widgets/ambient_glow.dart` — radial gradient animado
  (`AnimatedContainer` em cor com alpha), atrás do conteúdo.
- Create: `lib/shared/widgets/breathing_circle.dart` — ciclo 9s (4s inspira, 1s segura,
  4s expira) com cue textual sincronizado por `AnimationStatus`/valor.

## Task 3 — Feature Respirar
- Create: `lib/features/respirar/respirar_screen.dart` — BreathingCircle + Salmo 46·10,
  fundo nightBase com glow, fechar no topo. Analytics: screen view padrão.
- Modify: `lib/core/navigation/app_router.dart` → rota `/respirar` via `_slideUp`.

## Task 4 — Home V2
- Modify: `lib/features/home/home_screen.dart`:
  starfield atrás do scroll; entrada staggered (header → data → número → versículo → CTA);
  versículo-âncora com WordRevealText; card "Um momento — Respirar" antes do atalho de
  coleções.

## Task 5 — Coleções V2
- Modify: `lib/shared/widgets/collection_card.dart` → param opcional `ghostNumber`
  (numeral Playfair gigante, alpha baixo, canto inferior direito, cor do dot).
- Modify: `lib/features/colecoes/colecoes_screen.dart` → itens com StaggeredEntrance,
  ghostNumber = primeiro salmo da coleção.

## Task 6 — Leitura V2 (karaokê)
- Create: `lib/features/salmos/verse_sync.dart`:
  ```dart
  /// Índice do versículo ativo estimado pela posição do áudio,
  /// ponderado pelo comprimento de cada versículo.
  int activeVerseIndex({
    required Duration position,
    required Duration duration,
    required List<String> versiculos,
  })
  ```
- Modify: `lib/shared/widgets/verse_line.dart` → cor via `AnimatedDefaultTextStyle`
  (Duration.zero quando disableAnimations) + param `esmaecido`.
- Modify: `lib/features/salmos/leitura_salmo_screen.dart` → `_Body` observa
  `audioPlayerProvider`; tocando → versículo ativo em âmbar, demais em texto normal;
  parado → comportamento V1 (primeiro versículo em destaque).

## Task 7 — Teste
- Create: `test/features/salmos/verse_sync_test.dart` — posição 0 → 0; fim → último;
  ponderação por comprimento; duration zero → 0.

## Task 8 — Verificação e entrega
```bash
cd app-v2 && flutter analyze && flutter test
git add app-v2 && git commit && git push
```
